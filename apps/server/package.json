{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/main.js",
  "types": "./src/main.ts",
  "scripts": {
    "dev": "concurrently npm:dev:*",
    "dev:start": "mkdir -p dist && touch dist/main.js && node --watch-path dist --env-file=.env dist/main.js",
    "clean": "rm -rf dist",
    "start": "NODE_ENV=production node dist/main.js",
    "preview": "NODE_ENV=production node --env-file=.env dist/main.js",
    "build": "esbuild src/main.ts --platform=node --sourcemap --target=node20 --bundle --outdir=dist --loader:.html=text",
    "dev:build": "npm run build -- --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21"
  }
}